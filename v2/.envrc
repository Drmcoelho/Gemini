# .envrc — carrega .env e aplica aliases ao entrar na pasta (direnv)
dotenv .env

# aplicar aliases definidos na .env (linhas que começam com 'alias ')
if [ -f .env ]; then
  while IFS= read -r line; do
    case "$line" in
      alias\ *) eval "$line" ;;
    esac
  done < .env
fi

PATH_add .
