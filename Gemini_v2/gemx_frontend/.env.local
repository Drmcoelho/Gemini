HOST=0.0.0.0
PORT=3003

# By default, use CRA proxy (see package.json "proxy").
# Define as vazio para usar caminhos relativos (proxy do CRA).
REACT_APP_API_BASE=

# Se precisar apontar para outro endpoint, substitua a linha acima por:
# REACT_APP_API_BASE=https://seu-endpoint

